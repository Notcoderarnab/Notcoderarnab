#include <stdio.h>
#include <conio.h>

void main() {
    int number, a, i;
    printf("Enter the number: ");
    scanf("%d", &number);
    for (i = 1; i <= 10; i++) {
        a = number * i;
        printf("%d", a);
    }
    getch();
}

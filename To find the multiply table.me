#include <stdio.h>
#include <conio.h>

void main() {
    int number, a, i;
    printf("Enter the number: ");
    scanf("%d", &number);
    for (i = 1; i <= 10; i++) {
        a = number * i;
        printf("%d", a);
    }
    getch();
}

<!---
Notcoderarnab/Notcoderarnab is a ✨ special ✨ repository because its `README.md` (this file) appears on your GitHub profile.
You can click the Preview link to take a look at your changes.
--->
